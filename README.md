# QProtocol
This is the library that hybrids different communication ways in one network. It is currently supports Arduino, ESP8266, ESP32 boards. Also has quick-setup Serial Communication, ESP-NOW, NRF24 TX-RX functions. Other communication methods can usable when manually setup by user.
